/*
package main tells the Go compiler that the package should compile as an executable program instead of shared library

main function in the main package is the entry point of the program
*/
package main

import "fmt"


func main() {
	
}